description: "Eval — Cloze"

providers:
  - id: openai:chat:llama-3.1-8b-instant
    config:
      apiBaseUrl: https://api.groq.com/openai/v1
      temperature: 0

prompts:
  - file://prompt.txt

defaultTest:
  options:
    provider:
      id: openai:chat:llama-3.1-8b-instant
      config:
        apiBaseUrl: https://api.groq.com/openai/v1
        temperature: 0

tests:
  - file://eval_tests.yaml
  - file://testing_tests.yaml
